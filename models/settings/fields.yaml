# ===================================
#  Form Field Definitions
# ===================================

fields:
    enabled_buttons:
        span: left
        label: Buttons
        type: checkboxlist
    permissions:
        span: right
        label: Permissions
        commentAbove: Show Content Editor only for
